import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    top: 0,
    width: "100%",
    backgroundColor: "#000",
    alignItems: "center",
    justifyContent: "center",
    height: 173,
  },
  textStyle: {
    color: "#f2f2f2",
    fontSize: 24,
    fontWeight: "500",
    maxWidth: "90%",
  },
});
